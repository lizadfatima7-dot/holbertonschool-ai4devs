@"
# Dice Rolling Function

This mini project contains a simple dice-rolling function.

## Function
roll(sides) rolls a dice with the given number of sides and returns a random number between 1 and sides.

## Usage
const { roll } = require('./dice');
console.log(roll(6));
"@ | Out-File README.md -Encoding UTF8